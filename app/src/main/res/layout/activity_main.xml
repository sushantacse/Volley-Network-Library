<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">



    <androidx.recyclerview.widget.RecyclerView
        android:studentid="@+studentid/reyclerview"
        android:layout_width="match_parent"
        android:layout_margin="10dp"
        android:layout_above="@+studentid/search"
        android:layout_height="match_parent"/>


    <Button
        android:studentid="@+studentid/search"
        android:text="Search Student"
        android:onClick="gotosearch"
        android:layout_above="@+studentid/b"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:studentid="@+studentid/b"
        android:text="Add Student"
        android:onClick="gotoaddstudent"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />


</RelativeLayout>